#include<stdio.h>
#include<conio.h>
int check(int);
void main()
{
int n,r;
printf("\n enter a number=");
scanf("%d",&n);
r=check(n);
if(r==1)
{
printf("\n %d is neon number",n);
}
else
{
    printf("\n %d is not neon number",n);
}
}
int check(int p)
{
    int i,square,sum;
    for(i=0; i<=p; i++)
    {

    }
}