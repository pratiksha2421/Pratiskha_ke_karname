// WAp to input a string and calculate length of string.

#include<stdio.h>
#include<conio.h>
void main()
{
char name[50];
int i;
printf("\n enter string =");
gets(name);
for(i=0; name[i]!='\0';i++)
{
}
printf("\n enter length=%d",i);
}